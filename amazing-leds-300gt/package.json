{
  "name": "amazing-leds-300gt",
  "version": "0.1.0",
  "private": true,
  "description": "iRacing lighting profiles with per-car RPM for the Conspit 300 GT",
  "scripts": {
    "import:existing": "node tools/import-existing.cjs",
    "build": "node tools/build.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "npm run build && npm test"
  },
  "engines": { "node": ">=18" },
  "devDependencies": { "acorn": "8.14.1" }
}

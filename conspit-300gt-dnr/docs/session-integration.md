# 会话成果整合记录

整合日期：2026-09-13。
来源任务：[评估 DNR 模型器插件逆向](codex://threads/01a0962d-08a7-7390-af58-e108062985c5)。

用户在来源任务中最终选择了适用于 300GT 的全部灯效和车型，并提供 CONSPIT 官网
V1.6 Individual (ungrouped) 文件作为设备框架。最新成果是整轮 Standalone V2.0，
RPM FullColour/TDM 为它的前置阶段成果。

本仓库接续该任务的配置、最小构建输入、构建工具和验证工具。该记录汇集开发上下文；
两个原始任务的聊天记录仍保留在各自会话中。

已完成：更新 DNR 7.0.3 七游戏的 width-12 车型树；保留 V1.6 按键/旋钮/旗语/进站等
效果编排；内嵌红线和 TDM 辅助函数；修复 F1 SC/DRS 依赖及空值问题；保留通用回退。
红线使用与原生车型公式对应的原始 JS 表。C# 表仅供来源键和差异核对，不能直接替换 JS 表。

迁入时只修改构建路径、输出组织、报告位置与 RPM-only 的稳定 ID。
整轮 ProfileId 继续为 `3c70494a-f7ce-490d-98e4-9710341940a2`。
没有把 DNR 的 BMW 数据覆盖到 amazing-leds-300gt，也没有套用该项目的左到右临时映射。

边界：没有取得最新版 DNR 官方 300GT 设备效果配置；本项目是用户所提供 V1.6 框架
与 7.0.3 内置数据的组合，不代表 DNR 官方最新整轮产品。F12026 沿用原有字段，
兼容性未实测。没有进行实体方向盘或真实游戏验证。

后续优先导入整轮配置验证，记录实际使用的文件、车型、挡位、RPM、齿比及同步视频。
针对观测到的偏差修改对应项目源码，重新生成配置；不得把历史手册或另一项目数据
未经核对直接作为当前游戏的校准结果。

## 本次迁入验证

- 三个生成 `.ledsprofile` 的 SHA-256 与来源任务最后产物完全相同。
- `npm run check`：RPM 对照检查 13980 项、整轮脚本/结构检查 13519 项通过；
  另验证了来源哈希、三个独立 ProfileId、跨工作目录构建及六个生成文件的确定性。
- 本机 SimHub 原生反序列化/序列化成功；4205 个容器的数量、类型顺序和内嵌 JS 均保留，
  无 UnknownContainer；365 条 NCalc 公式解析通过。
- 原有 amazing-leds-300gt 的 26 项测试通过。

上述为 2026-09-13 本次迁入的实际验证，不是仅引用来源任务的历史报告。
详细运行报告在 `.validation/` 中；未进行 SimHub UI 导入、真实遥测或实体点灯测试。
